import { CalendarCurrencyPipe } from './calendar-currency.pipe';

describe('CalendarCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new CalendarCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
