import { HoverOpacityDirective } from './hover-opacity.directive';

describe('HoverOpacityDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverOpacityDirective();
    expect(directive).toBeTruthy();
  });
});
