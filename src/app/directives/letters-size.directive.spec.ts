import { LettersSizeDirective } from './letters-size.directive';

describe('LettersSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new LettersSizeDirective();
    expect(directive).toBeTruthy();
  });
});
