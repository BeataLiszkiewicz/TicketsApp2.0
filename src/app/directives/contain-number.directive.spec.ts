import { ContainNumberDirective } from './contain-number.directive';

describe('ContainNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainNumberDirective();
    expect(directive).toBeTruthy();
  });
});
